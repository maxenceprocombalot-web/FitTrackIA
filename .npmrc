# Aligne l'installation des serveurs EAS sur l'installation locale.
# Sans ceci, npm échoue en ERESOLVE au build (phase « Install dependencies ») :
# certaines dépendances transitives réclament react-dom en peer.
legacy-peer-deps=true
